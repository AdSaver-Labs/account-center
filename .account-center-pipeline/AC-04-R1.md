# AC-04-R1 — Reauth replay receipt fidelity
State: PLANNED
Candidate: main
Owner: Account Center production coordinator
No-touch: live accounts, credentials, routing, active sessions, provider state, system services, Sentinel `/auth` rendering, Hermes/Dexter weekly-only policy, owned native delete transaction, package manifests/lockfiles, and unrelated worktrees.

## Bounded blocker-resolution plan
1. Preserve the merged AC-06 owned-delete receipt boundary and inspect only the current reauth mutation-repository write/read/replay path; do not change runtime adapters or execute native commands.
2. Add fixture-only regressions that construct persisted credential-blind operations for failed, unproven, and verified reauth outcomes, then assert idempotent replay returns the original verification and route categories without re-invoking dependencies.
3. Implement the smallest serialization/deserialization correction needed to retain fixed verification/route categories; reject malformed, target-bearing, credential-bearing, or unsupported persisted evidence before public rendering.
4. Run focused reauth/mutation repository tests, then `npm run qa` and `npm run qa:security`; statically compile only relevant Python fixtures if required, never execute the owned delete helper.
5. Review the diff for redaction, opaque receipt parity, byte-stable Sentinel `/auth`, working Dexter `/auth delete`, and Hermes/Dexter weekly-only invariants. Record only factual evidence; merge only after all review and QA gates pass.

## Acceptance evidence
- Replays of failed and `UNPROVEN` reauth retain the stored verification category and `not_requested` route state.
- Replays of verified `applied`, `not_applied`, and `UNPROVEN` route outcomes retain their original truthful category without re-running stage, verification, or route actions.
- Persisted/public evidence remains credential- and target-free; malformed evidence fails closed.
- No interactive login, provider request, credential/store write, route mutation, session/service operation, native helper execution, or live deletion occurs.

## Review results
- Spec: approved — persisted mutation evidence is now an explicit closed schema at every nested proof boundary. Target-bearing, credential-bearing, or otherwise unsupported evidence makes the repository corrupt and prevents replay/rendering.
- Quality: passed — `npm test` passed: 257 Node fixture/mock tests and 18 Hermes-plugin tests. The added fixtures replay a failed/unproven historical operation without an adapter and reject persisted target/token fields.
- QA: passed — `npm run qa` passed (typecheck, build, full fixture tests, and 15 Chromium/axe checks); `npm run qa:security` passed (141 tracked files secret-scanned; `npm audit --audit-level=high`: 0 vulnerabilities).
- Preservation review: existing fixture coverage passed for the fixed owned exact-account delete transaction, its single opaque `opaque-owned-delete` receipt, Hermes/Dexter shared transport, Sentinel `/auth` rendering, and weekly-only policy. No native helper, live deletion, login, provider access, credential/store write, route mutation, or service operation ran.
