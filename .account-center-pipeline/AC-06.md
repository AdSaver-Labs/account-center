# AC-06 — owned exact-account credential delete
State: MERGED
Candidate: main
Owner: Account Center production coordinator
No-touch: live accounts, credentials, routing, sessions, prompt/memory/bootstrap, production runtime stores, Sentinel full `/auth` format, and Hermes/Dexter weekly-only policy.

## Candidate
Account Center invokes Dexter's one owned exact-account transaction at `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py` only after shared review/capability/idempotency checks and an exact connected identity resolution. Hermes continues to call the canonical Account Center `/auth delete` transport; both public boundaries accept only the shared versioned `opaque-owned-delete` contract or its fixed `UNPROVEN` counterpart.

## Acceptance evidence
- The owned transaction was verified without execution as a regular, owner-only (`0600`) file owned by `Alej` (uid 1001), with SHA-256 `4c09c926e94500f02f34a19ca80fbec280003227588d2b4f0d1d1085ee7fba37`, matching the adapter pin.
- Account Center fixtures prove the exact native argv, canonical connected-email/profile-id matching, target-free public receipt, and fail-closed behavior for missing/untrusted helpers, malformed or mismatched receipts, nonzero exit, timeout, and output overflow.
- Hermes fixtures replace `subprocess.run` before `/auth delete` is exercised, assert the same canonical ChatOps command and two-output contract, and fail closed for injected output, nonzero transport, and unavailable transport. No native delete transaction can run in these tests.
- Existing Hermes/Sentinel capacity fixtures retain the weekly-only account capacity gate and fail closed for missing/unproven pairing evidence. `/auth` normal status-format regression tests passed.

## Review results
- Spec: passed — `OpenClawRuntimeAdapter` has the fixed owned-script path, validates its regular-file/owner/mode/hash trust boundary immediately before invocation, and consumes only action/state/backup/verified plus a locally checked target digest. It returns no native path, target digest, account identity, or diagnostics. The Hermes bridge loads the same versioned contract and forwards only its two exact public strings.
- Quality: passed — full `npm run test` passed: 259 Node fixture/mock tests and 18 Hermes-plugin tests. Relevant tests assert shared review/idempotency evidence, `opaque-owned-delete` parity across Dexter/Hermes/MCP, exact invocation with mocked runner, target ambiguity rejection, and no direct/live adapter apply.
- QA/security: passed — `npm run qa:security` passed including typecheck/build, the full fixture suites, 15 Playwright/axe accessibility tests, a 145-file secret scan, and `npm audit --audit-level=high` with 0 vulnerabilities. `git diff --check` and the AC-06 review checkpoint passed.

No live deletion, route mutation, provider access, interactive login, credential/store write, or service operation was performed.
