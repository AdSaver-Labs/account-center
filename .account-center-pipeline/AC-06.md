# AC-06 — Owned exact-account delete transaction parity
State: PLANNED
Candidate: main
Owner: Account Center production coordinator

## Non-negotiable boundary

The locally owned native exact-account delete transaction is `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`. It is the transaction used by Dexter `/auth delete`; absence of an upstream OpenClaw CLI delete command is not a blocker. No test may execute this helper or perform a live deletion.

Preserve byte-for-byte Sentinel `/auth` status behavior, the Hermes/Dexter weekly-only eligibility policy, and the working Dexter command. Public completion evidence is exactly the target-free opaque contract `{"action":"account.delete","state":"DELETED","receipt":"opaque-owned-delete"}`; private target digests, native stderr, backup paths, and store data must never cross the adapter boundary.

## Bounded execution plan

1. Reconcile the Account Center OpenClaw adapter with the owned native helper path, strict exact-connected-target resolution, executor capability, and allow-listed opaque receipt parser.
2. Re-run fixture-only adapter tests that assert the helper invocation shape, normalized exact identity selection, receipt opacity, and fail-closed handling for malformed, mismatched, timed-out, or unverified native results. Test runners are fakes; the native Python helper must not run.
3. Re-run fixture-only Hermes MCP/Dexter ChatOps transport tests proving that only the canonical applied or unproven delete text is forwarded and that injected target/path material is rejected.
4. Run deterministic repository review, type/build/test, security QA, and static compilation of the owned Python helper only. Review Sentinel `/auth` fixtures and policy tests for unchanged locked format and weekly-only behavior.
5. Record spec, quality, and QA verdicts under the exclusive pipeline-state lock. Merge only if all pass; otherwise retain this candidate and replace this plan with a materially different bounded remediation plan.

## Explicit exclusions

No live account, credential, route, Sentinel, provider, session, service, browser, or store operation; no native-helper execution; no direct JSON/SQLite edit; no status probe; no package/lockfile change.
