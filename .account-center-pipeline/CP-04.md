# CP-04 — No-secret assurance: redacted connection references
State: PLANNED
Candidate: main working-tree candidate (preserved; no reset, checkout, or live runtime operation)
Owner: Account Center production coordinator

## Bounded blocker-resolution plan
1. Preserve and inspect the existing candidate as an internal filesystem-safety remediation; do not discard or widen it. Limit scope to public connection inventory and Hermes automation-capacity exports.
2. Replace public connection/scope-derived identifiers with deterministic opaque references, and ensure scoped leases/onboarding payloads do not reveal scopes, commands, provider identities, credentials, or short-window capacity.
3. Add adversarial fixture-only tests using hostile but syntactically valid scope text, plus stability tests proving reordering cannot create cross-agent capacity transitions.
4. Run static TypeScript build, focused fixture tests, full deterministic QA/security gates, and Python compilation only for the owned delete helper. Never execute the helper or conduct a live deletion.
5. Independently review the diff for disclosure regressions and confirm the locked Sentinel `/auth` status rendering, working Dexter command, AC-06 opaque receipt contract, and Hermes/Dexter weekly-only policy remain unchanged. Merge only with all evidence green.

## Acceptance evidence
- Public inventory, lease, and automation exports contain no raw scope, command, email/profile, token/secret, or five-hour fields.
- Stable opaque references prevent spurious capacity-transition notifications after ordering changes.
- The AC-06 owned `codex-auth-delete.py --apply` boundary is unchanged and is exercised only through fake-runner/fixture tests.
- Sentinel `/auth` normal status fixtures and weekly-only capacity fixtures remain green.
- No live provider, credential, routing, session, service, browser, or native-delete operation occurs.

## Review results
- Spec: passed — public connection inventory, leases, and Hermes automation exports now retain only deterministic opaque connection references; private adapter scopes remain necessary server-side but have no public field or command representation. The AC-06 exact-target owned delete boundary remains unchanged.
- Quality: passed — focused adversarial fixtures reject hostile scope text and prove stable references across reordering, while the diff removes public five-hour capacity and scope-derived command output. `git diff --check` passed; the locked Sentinel `/auth` normal-status fixture and canonical Hermes/Dexter delete-transport fixtures remained green.
- QA: passed — `npm run qa` and `npm run qa:security` passed: 249 fixture/mock tests, typecheck/build, 15 Playwright/axe tests, secret scan, and audit (0 high vulnerabilities). The owned helper was compiled only; it was not executed, and no live deletion or provider/runtime mutation occurred.
