# CP-01 — Loopback and per-launch token proof
State: PLANNED
Candidate: main
Owner: Account Center production coordinator
No-touch: `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`; live accounts, credentials, routing, sessions, system services, provider endpoints, Sentinel `/auth` renderer/format, working Dexter `/auth delete`, Hermes/Dexter weekly-only policy, production runtime stores, package manifests/lockfiles, and unrelated worktrees.

## Bounded micro-tasks
1. Inventory the server launch/listen lifecycle and existing fixture-only tests for host selection, token generation, output redaction, and cleanup.
2. Add targeted fixture tests for default loopback binding, independently generated per-launch bearer tokens, token-free startup/reporting, and deterministic close/cleanup behavior.
3. Make the minimum server/CLI lifecycle change required to fail closed on unsafe host, token, or lifecycle evidence; do not add a runtime mutation surface.
4. Prove protected API access accepts only the launch token and public/help/status output never serializes it.
5. Re-run the owned AC-06 opaque-delete fixture suites and byte/behavior invariants for Sentinel `/auth`, Dexter command parity, and Hermes/Dexter weekly-only capacity.
6. Run focused tests, full deterministic QA/security/browser proof, staged diff review, and independent spec/quality/QA review before any merge decision.

## Acceptance evidence
- The local control plane binds loopback by default, creates a fresh per-launch token, never logs that token, and cleans up predictably.
- No request can broaden the bind host or reuse/derive a token without an explicit safe server configuration contract.
- AC-06 continues using only the owned exact-account transaction and target-free `opaque-owned-delete` receipt; tests remain fixtures-only and never execute the native helper.
- Sentinel `/auth`, working Dexter delete behavior, and Hermes/Dexter weekly-only policy remain unchanged.
- No live account, credential, route, session, provider, or service operation occurs.

## Review results
- Spec: passed — `server.listen` has no host input and binds only `127.0.0.1`; the CLI accepts exactly one owner-only token-file source and has no direct token or host override. Protected API access accepts the supplied bearer token and unauthenticated access is rejected. Startup output reports neither the token nor its pathname.
- Quality: passed — fixture coverage verifies independent owner-only token generation, unsafe/missing/repeated/symlinked token-file inputs fail before launch, output remains token-free, fixture server cleanup completes, and the canonical Hermes/Dexter delete transport permits only the two versioned opaque results. The locked Sentinel `/auth` rendering and weekly-only capacity behavior were not changed.
- QA: passed — current release reconciliation passed `npm run qa:security`: 262 Node fixture/mock tests, 19 direct Hermes-plugin tests, TypeScript typecheck/build, 15 Playwright/axe checks, a 154-file secret scan, and `npm audit --audit-level=high` with 0 vulnerabilities. Static `py_compile` passed for the Hermes plugin and the owned helper; `git diff --check` passed. No native helper execution, live deletion, non-fixture server launch, login, provider request, credential/store, routing, model, session, runtime, or service operation occurred.

Result: MERGED after CP-01 R1 spec, quality, QA/security, and preservation reconciliation.
