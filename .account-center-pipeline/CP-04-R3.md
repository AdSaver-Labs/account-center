# CP-04 R3 — preservation-evidence path correction

State: PLANNED
Owner: Account Center production coordinator

## Internal issue

The current CP-04 release record is merged and the full fixture/security gate is
passing, but an independent preservation probe used a removed adapter filename.
That probe failed before examining the actual `runtime-adapters.ts` owner path.
This is an internal evidence-integrity defect, not an external dependency and
not a reason to skip or reclassify AC-06.

## Bounded resolution plan

1. Retain the merged CP-04 candidate and under the exclusive pipeline lock mark
   this evidence correction `PLANNED`; change neither the owned helper nor any
   runtime account, credential, routing, or provider state.
2. Replace the obsolete filename assertion with a source-to-consumer topology
   check: `runtime-adapters.ts` must pin the sole owned helper, while CLI,
   ChatOps/Dexter, MCP, Hermes, and the versioned contract must each reference
   the same opaque receipt boundary.
3. Re-run that topology probe and the complete fixture-only security QA gate,
   plus owner/mode/digest and compile-only validation of the helper. Do not run
   the helper, login, contact a provider, or mutate any live store.
4. Independently review a clean diff and verify the full Sentinel `/auth`
   format, working Dexter `/auth delete`, exact-target behavior, and
   Hermes/Dexter weekly-only policy. Record a merge only if all evidence is
   green; otherwise preserve this candidate as `PLANNED` with the failure.

## Fixed invariants

- AC-06 is the owned local transaction at
  `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`;
  lack of an upstream CLI delete command is irrelevant.
- `account-center.owned-delete-receipt.v1` / `opaque-owned-delete` is the sole
  target-free public delete receipt contract.
- All destructive tests are fixtures/mocks only. Live deletion is forbidden.
