# SC-03 — Model policy truth model
State: MERGED
Worktree: /tmp/account-center-SC-03
No-touch: /home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py; live OpenClaw/Hermes stores; Sentinel `/auth` renderer/format; Hermes/Dexter weekly-only capacity policy; credentials, sessions, prompts, and memory.

## Micro-tasks
1. Inventory every CLI, API, MCP, ChatOps, UI, audit, and adapter surface that reads or renders a model catalog, requested policy, effective runtime model, fallback chain, eligibility, or verification state.
2. Write fixture-only red tests for missing, malformed, stale, contradictory, and unsupported model evidence; assert each concept remains distinct and unavailable facts render as `not_reported` or `UNPROVEN` rather than a guessed default.
3. Define one minimal typed public evidence contract that separates observed catalog facts from requested policy, effective runtime selection, fallback configuration, eligibility rationale, and authoritative verification proof.
4. Normalize trusted adapter inputs into that contract without reading credentials or deriving provider capacity from Hermes login text; retain weekly-only canonical capacity behavior.
5. Update the protected CLI/API/MCP/ChatOps/UI read paths to render the same redacted model truth, explicitly preserving read-only/unsupported boundaries where no native mutation proof exists.
6. Add adversarial fixture tests covering hostile labels, cross-runtime/scope evidence borrowing, disabled-versus-unavailable models, fallback absence, and audit/receipt redaction.
7. Run focused tests, full deterministic `npm run qa`, `npm run qa:security`, and browser/a11y proof; compile-check the owned Python delete transaction only and perform no live deletion or runtime mutation.
8. Obtain independent spec, quality, and QA verdicts; review staged diff/invariants, commit, push, and record only actual evidence if every required gate passes.

## Acceptance evidence
- Catalog, requested policy, effective runtime model, fallback chain, eligibility, and verification are independently typed, rendered, and fixture-tested.
- Missing or non-authoritative evidence cannot become an inferred selection, fallback, eligibility, or verified state.
- All outputs remain redacted and all runtime/scope claims remain bound to their authoritative source.
- Sentinel `/auth` format, working Dexter `/auth delete` owned transaction, opaque native delete receipt contract, and Hermes/Dexter weekly-only policy are byte/behavior invariant.
- No live deletion test or live runtime mutation occurs.

## Review results
- Spec: passed — the shared `account-center.models.v1` projection keeps observed catalog, requested policy, effective runtime selection, fallback chain, eligibility, and verification distinct. It contains only allow-listed model IDs; selection/fallback remain `not_reported` and verification remains `UNPROVEN` without authoritative runtime evidence. A requested but unsupported runtime produces zero observations rather than borrowing another runtime's evidence.
- Quality: passed — fixture-only adversarial coverage exercises malformed profile/policy shapes, invalid timestamps, stale unreadable evidence, contradictory disabled/catalog facts, hostile private labels, cross-runtime observations, and an unsupported runtime. CLI/API, MCP/ChatOps, and the control panel use the same protected read-only projection. The full suite passed: 262 Node tests and 19 Hermes-plugin tests.
- QA/security: passed — `npm run qa:security` passed typecheck/build, deterministic fixtures, 15 Playwright/axe tests, a 153-file secret scan, and `npm audit --audit-level=high` with 0 vulnerabilities. `git diff --check` and static `py_compile` passed for the Hermes bridge and owned delete helper.
- Preservation: passed — fixture tests retained the one owned exact-account delete transaction, exact opaque two-outcome receipt contract across Dexter/ChatOps, MCP, and Hermes, normal Sentinel `/auth` output, and weekly-only capacity behavior. No owned helper execution, live deletion, interactive login, provider request, credential/store write, route mutation, runtime mutation, or service operation occurred.

Result: MERGED after coordinator spec, quality, QA/security, and preservation review.
