# AC-03 — Durable guided-auth lifecycle verification gate

State: MERGED
Candidate: main after AC-06 receipt-contract integrity merge
Owner: Account Center production coordinator

## Non-negotiable boundary

Preserve the locked Hermes/Dexter Sentinel `/auth` status format, the Hermes/Dexter weekly-only capacity policy, the owned exact-account delete transaction and its opaque receipt contract, and the working Dexter `/auth delete` command. All validation is fixture-only: do not perform interactive login, provider/runtime mutation, routing change, credential-store modification, native-helper execution, or live deletion.

## Bounded execution plan

1. Preserve the verified AC-02 candidate and inspect the existing local guided-auth lifecycle boundary only: durable store, guarded executor, protected API/CLI/MCP projections, and their fixture tests.
2. Verify that lifecycle transitions remain local and non-terminal unless independently verified runtime evidence is available; malformed, expired, repeated, hostile, or proof-bearing records must fail closed and never retain raw targets or credential material.
3. Add only a narrowly scoped fixture test if a concrete lifecycle/receipt projection gap is demonstrated. Do not broaden into challenge completion, live login, routing, or native deletion.
4. Run focused fixture tests followed by deterministic type/build/security/browser QA; compile the owned Python helper only if relevant, never execute it.
5. Record spec, quality, and QA results under the exclusive pipeline-state lock. Merge only when all are green; otherwise preserve the candidate, replace this plan with a materially different bounded remediation plan, and remain PLANNED.

## Acceptance evidence

- Durable guided-auth records and public lifecycle views are redacted and contain no raw account target, OAuth/device payload, credential, token, proof, or forged verified state.
- Invalid or unsafe lifecycle state fails closed before public projection or durable mutation.
- Locked Sentinel `/auth`, weekly-only capacity, AC-06 owned-delete receipt parity, and Dexter command fixture contracts remain unchanged.
- No live account, provider, runtime, credential, route, session, service, browser, or native-delete operation occurs.

## Review results

- Spec: passed — durable guided-auth records accept only redacted non-terminal lifecycle fields; malformed, expired, duplicate, hostile, proof-bearing, verified, or terminal records fail closed. Public API, CLI, and MCP projections remain target-free.
- Quality: passed — the focused lifecycle, compiled-artifact, `/auth`, server, Sentinel-format, weekly-only capacity, and owned-delete receipt-contract fixture suite passed: 260 Node fixture/mock tests and 19 Hermes-plugin tests. No protected boundary was broadened.
- QA: passed — `npm run qa:security` passed typecheck/build, the same 260 Node and 19 Hermes-plugin fixture tests, 15 Playwright/axe tests, a 146-file secret scan, and `npm audit --audit-level=high` with 0 vulnerabilities. Static `py_compile` of the owned helper and Hermes plugin passed; neither was executed as a native transaction.
- No interactive login, provider/runtime mutation, native delete, live credential deletion, routing change, or Sentinel/Hermes/Dexter policy change occurred.
