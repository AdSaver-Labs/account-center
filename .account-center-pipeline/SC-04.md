# SC-04 — Honest state taxonomy completion
State: PLANNED
Candidate: main
Owner: Account Center production coordinator
No-touch: `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`; live accounts, credentials, routing, active sessions, system services, provider endpoints, Sentinel `/auth` renderer/format, working Dexter `/auth delete`, Hermes/Dexter weekly-only capacity policy, production runtime stores, package manifests/lockfiles, and unrelated worktrees.

## Bounded micro-tasks
1. Inventory CLI, API, MCP, ChatOps, and control-panel renderers that expose capability or model/action state, identifying any ambiguous success-like label for unsupported, unentitled, read-only, unknown, or unproven evidence.
2. Define one fixture-only public taxonomy projection with terminal, target-free meanings and explicit non-success semantics for every unavailable state.
3. Add adversarial fixtures for malformed capability records, absent entitlement evidence, read-only runtime declarations, unknown runtime labels, and attempted-but-unproven outcomes; ensure no state is upgraded by a stale or cross-runtime observation.
4. Apply the minimum shared renderer/projection change so CLI/API/MCP/ChatOps and UI consume the same honest state meanings without changing runtime mutation authority.
5. Preserve the owned AC-06 exact-account adapter invocation and one `opaque-owned-delete` receipt contract; compile-check only, never execute, the owned helper.
6. Run focused fixture tests, full deterministic QA/security/browser proof, byte/behavior checks for Sentinel `/auth` and Hermes/Dexter weekly-only capacity, then independent spec/quality/QA review.
7. Review only staged changes, run `git diff --check`, commit, push, and update the release gate only with actual proof.

## Acceptance evidence
- Unsupported, unentitled, read-only, unknown, and `UNPROVEN` are visibly and programmatically distinct, terminal where appropriate, and never success-like.
- A malformed, stale, or cross-runtime fixture cannot claim an available or verified action.
- Sentinel `/auth`, working Dexter delete behavior, owned delete transaction/opaque receipt, and weekly-only capacity policy remain unchanged.
- No live deletion, credential/routing/model mutation, provider request, session change, or service operation occurs.

## Review results
- Spec: passed — fixture-only taxonomy is terminal, target-free, and non-success; malformed/absent capability evidence fails closed.
- Quality: passed — the owned OpenClaw transaction pin and single `opaque-owned-delete` boundary remain intact; Sentinel `/auth` and Hermes/Dexter weekly-only sources were untouched.
- QA: passed — full deterministic suite (245 tests), security gate, and 15 Playwright/axe tests passed; owned helper syntax was compiled only and never executed.
