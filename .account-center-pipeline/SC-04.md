# SC-04 — Honest state taxonomy
State: PLANNED
Candidate: main
Owner: Account Center production coordinator
No-touch: `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`; live accounts, credentials, routing, sessions, system services, Sentinel `/auth` format, working Dexter command, Hermes/Dexter weekly-only policy, and provider endpoints.

## Bounded micro-tasks
1. Inventory public CLI, ChatOps, MCP, Hermes bridge, API, and browser renderers for capability and mutation states; identify places where absent evidence can appear as success or a supported action.
2. Add a minimal typed taxonomy for `unsupported`, `unentitled`, `read_only`, `unknown`, and `UNPROVEN`, retaining the existing exact owned-delete opaque-receipt contract unchanged.
3. Add fixture-only adversarial tests for malformed/absent runtime capability evidence, attempted-but-unverified mutations, and cross-surface canonical rendering; assert no native delete helper is reached.
4. Make only the smallest core and presentation changes needed to render the same truthful state across protected command/API/ChatOps/Hermes paths, without extending mutation authority.
5. Run focused tests, full typecheck/build/QA/security/a11y suites, owned-helper compile-only check, static Sentinel/Dexter/Hermes invariant checks, then perform spec, quality, and QA review before merge.

## Acceptance evidence
- Each taxonomy state is distinct, explicit, target-free, and fixture-tested at public boundaries.
- Missing or malformed evidence fails closed as `unknown` or `UNPROVEN`; it never grants capability or renders success.
- The sole native credential delete executable remains the immutable owned Python transaction and only its existing verified opaque receipt can render applied.
- No live deletion, account/routing mutation, provider request, interactive login, or service operation occurs.

## Review results
- Spec: pending
- Quality: pending
- QA: pending
