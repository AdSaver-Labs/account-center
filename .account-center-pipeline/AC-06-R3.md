# AC-06-R3 — CI-portable owned-helper fixture seam
State: PLANNED
Candidate: `3f59a82`
Owner: Account Center production coordinator
No-touch: owned helper implementation, live accounts/credentials/routes/sessions/stores, Sentinel `/auth`, Dexter command semantics, Hermes/Dexter weekly-only policy.

## Bounded micro-tasks
1. Preserve the shipped candidate and keep the production owned-helper validator fail-closed.
2. Correct only fixture adapters that intentionally model a trusted helper so they do not inspect this host-only helper path on GitHub runners.
3. Re-run local full tests and QA, then push the narrow fixture portability correction.
4. Verify the GitHub Actions run for the correction; do not advance to a new release gate unless it is green.

## Acceptance evidence
- Production still requires the fixed owner/type/mode/hash validation.
- The only mocked trusted-helper evidence is inside fixture tests; no test invokes the native helper.
- Local and GitHub test matrices pass without depending on the coordinator host path.
