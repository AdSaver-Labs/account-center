# UI-02 — Complete accessible action UI

State: MERGED
Owner: Account Center production coordinator

## Preserved baseline

UI-01 R3 is merged at `c01fa3d`. The owned exact-account transaction at
`/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`,
its sole target-free opaque receipt contract, full Sentinel `/auth` formatting,
working Dexter `/auth delete`, and Hermes/Dexter weekly-only policy are locked.

## Bounded execution plan

1. Inventory the current native visual control-panel actions and the protected
   API contracts they represent; classify every exposed action as read-only,
   guarded preview/confirmation, unavailable, or `UNPROVEN`.
2. Add only fixture-server browser/keyboard assertions for status, routing,
   guided add/reauth, models, receipts/audit, and destructive-confirmation
   states. No test may launch an owned helper or contact a runtime/provider.
3. Implement the smallest UI-only changes necessary to make those actions
   accessible, responsive, and truthful. Do not alter mutation authority,
   adapter integration, `/auth` transport, or account data stores.
4. Independently review public redaction and preservation of the AC-06 opaque
   contract, Sentinel format, Dexter command, and weekly-only policy; run full
   fixture/mock QA-security and helper compile-only validation.
5. Merge only after spec, quality, QA/security, and accessibility evidence are
   green; otherwise retain the candidate, write a distinct bounded internal
   resolution plan, and remain `PLANNED`.

## Non-negotiable prohibitions

No live deletion, native helper execution, interactive login, provider request,
credential/store write, route/model mutation, live browser action, or service
operation. Fixture servers, mocks, and compile-only checks are permitted.

## Execution and release evidence

- Spec: passed — the existing fixture-only control-panel server covers status,
  routing, guided-auth, models, receipts/audit, protected operation details,
  destructive-action availability, keyboard navigation, focus restoration, and
  truthful `blocked`, read-only, and `UNPROVEN` states. It exposes no native
  delete control or runtime mutation authority.
- Quality: passed — focused adapter/CLI/MCP/owned-delete documentation fixtures
  passed (110 tests), and the Hermes plugin fixture suite passed (19 tests).
  Mocked subprocess tests prove Account Center and Hermes/Dexter retain the
  exact `opaque-owned-delete` receipt contract and canonical working `/auth
  delete` transport without launching the owned helper.
- QA/security: passed — `npm run qa:security` passed: build/typecheck, 268
  Node fixture/mock tests, 19 Hermes-plugin tests, 15 Playwright/axe
  fixture-browser tests, 166-file secret scan, and `npm audit` with zero high
  vulnerabilities. The owned helper was compile-checked only; it is an
  Alej-owned mode-0600 regular file with the adapter-pinned SHA-256.
- Review: passed — `c01fa3d` equaled fetched `origin/main` before this
  coordinator-only release record; the protected contract, adapter, ChatOps,
  and MCP files are unchanged from that revision. Full Sentinel `/auth`,
  Dexter `/auth delete`, and Hermes/Dexter weekly-only policy remain locked.
