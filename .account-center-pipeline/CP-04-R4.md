# CP-04 R4 — ignored-evidence admission correction

State: PLANNED
Owner: Account Center production coordinator

## Internal issue

CP-04 R3 verification passed, but the release-evidence files are intentionally
ignored and a normal `git add` therefore refused to stage them. This is a local
repository-admission issue, not an external dependency and not a defect in the
owned deletion transaction.

## Bounded resolution plan

1. Preserve the R3 candidate and re-enter `PLANNED` under the exclusive
   pipeline lock; do not modify production code or any runtime state.
2. Inspect the staged-file allowlist and force-add only the three reviewed
   pipeline-evidence files: the R3 topology record, this R4 record, and
   `state.json`.
3. Commit, push, and verify exact `origin/main` equality. Re-read the committed
   state and confirm that the only released change is evidence metadata.
4. Do not re-run native actions: R3's fixture-only QA, topology proof, and
   compile-only helper validation remain the release evidence.

## Fixed invariants

The local `codex-auth-delete.py` owned transaction, target-free
`opaque-owned-delete` receipt, full Sentinel `/auth` format, Dexter `/auth
delete`, and Hermes/Dexter weekly-only policy are unchanged. No live deletion
or provider/runtime mutation is permitted.
