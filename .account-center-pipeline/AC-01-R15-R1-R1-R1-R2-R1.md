# AC-01-R15-R1-R1-R1-R2-R1 — route-selector parser isolation
State: PLANNED
Worktree: /tmp/account-center-AC-01-R15-R1-R1-R1-R2-R1
No-touch: main; parent candidate and every other worktree; docs/RELEASE_GATE.md; UI/onboarding; runtime adapters; live credentials/accounts/routing/services/Sentinel.

## Micro-tasks
1. Reproduce provider/runtime option semantic changes in non-`routes next` commands.
2. Define the sole parser/capability boundary for `routes next` selector validation.
3. Keep generic option parsing behavior byte-for-byte compatible for unrelated commands.
4. Ensure malformed/repeated route selectors preempt malformed source only for `routes next`.
5. Preserve split-form and equals-form valid `routes next` exact routing.
6. Add parity regressions for provider probe, guard, and dry-run mutation command option handling.
7. Add opaque text/JSON no-leak regressions for malformed route selectors.
8. Run focused tests, typecheck, security QA, diff check, commit, and push.
9. Require exactly one exact `(provider, runtime)` route before `routes next` delegates selection.
10. Add redacted text/JSON duplicate-route regressions with hostile route identities.

## Acceptance evidence
- The single redaction/capability boundary is `routes next` selector validation.
- No non-`routes next` command changes provider/runtime option semantics.
- Malformed/repeated `routes next` selectors always yield fixed opaque `UNPROVEN` code 2, including with malformed `--source`.
- Zero or multiple exact `(provider, runtime)` routes yield the same fixed opaque `UNPROVEN` code 2; only one exact route may select a profile.

## Review results
- Spec: implemented; selector boundary now requires exactly one exact route.
- Quality: redacted text/JSON duplicate-route regressions added; split/equals and non-`routes next` parity remain covered.
- QA: passed — focused route selector regressions; `npm test` (188/188); `npm run qa:security`; `git diff --check`.
