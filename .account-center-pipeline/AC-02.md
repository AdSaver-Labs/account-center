# AC-02 — Durable distinct `/auth add` and `/auth reauth` start

State: MERGED
Candidate: main
Worktree: main verification candidate (no source mutation was needed; focused fixture evidence proved the boundary)
No-touch: Hermes/Dexter locked Sentinel `/auth` format; owned native delete transaction; delete receipt contract; weekly-only capacity policy; all live provider/runtime stores.

## Micro-tasks
1. Preserve the AC-06 merged candidate and inspect the existing guided-auth command, executor, API, CLI, MCP, and durable repository paths against the AC-02 acceptance boundary.
2. Establish fixture-only evidence that `/auth add` and `/auth reauth` use distinct explicit modes, runtime/scope validation, idempotency, and redacted durable results.
3. Add or repair only a focused fixture test if a concrete AC-02 boundary is missing; do not broaden into challenge completion, live login, reauth, routing, or credential storage.
4. Verify malformed input, duplicated idempotency keys, unsupported scope/runtime, and hostile values fail closed without retaining raw targets or credentials.
5. Verify protected CLI/MCP/API views render only the redacted lifecycle contract and preserve the locked Hermes/Dexter command transport.
6. Run focused fixture tests, then deterministic QA/security and browser proof where an API/UI observable changes.
7. Obtain independent spec, quality, and QA verdicts; merge only if all proof is green.

## Acceptance evidence
- `/auth add` and `/auth reauth` create distinct redacted durable challenges through the shared guarded lifecycle.
- No credential, raw provider target, or OAuth/device payload is persisted or publicly rendered.
- Idempotency is exact and durable; malformed or unsupported scope/runtime inputs remain blocked.
- All verification uses fixtures/mocks; no interactive login, provider mutation, routing change, or native deletion is executed.

## Review results
- Spec: passed — `/auth add` and `/auth reauth` retain explicit distinct modes through the shared guarded local-intent lifecycle; runtime/provider/scope validation is exact, duplicate canonical input reuses the same durable challenge, and a different mode receives a different challenge. No interactive authentication or runtime mutation is part of this boundary.
- Quality: passed — static review and fixture coverage show durable and public challenge projections exclude targets, tokens, OAuth/device material, proof, and forged verification state. Malformed mode, scope/runtime/provider, cross-origin, and hostile persisted records fail closed. The locked Sentinel `/auth` rendering, weekly-only policy, owned delete helper path, opaque receipt contract, and Dexter transport remain unchanged.
- QA: passed — `npm run qa:security` passed 249 fixture/mock tests, typecheck/build, 15 fixture-only Playwright/axe tests, secret scan, and `npm audit` (0 high vulnerabilities). Hermes plugin fixtures (8) and static `py_compile` of the owned helper passed. No native helper execution, live deletion, interactive login, provider/runtime mutation, routing change, or live store operation occurred.
