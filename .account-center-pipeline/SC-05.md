# SC-05 — AC-06 release-gate reconciliation
State: MERGED
Candidate: main
Owner: Account Center production coordinator
No-touch: `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`; live accounts, credentials, routing, sessions, system services, provider endpoints, Sentinel `/auth` format, working Dexter command, Hermes/Dexter weekly-only policy, and any production runtime store.

## Bounded micro-tasks
1. Reconcile public strategy documentation with the already implemented AC-06 owned native exact-account delete adapter; remove only the stale claim that delete is blocked because no native transaction exists.
2. Add a documentation-contract fixture that proves the public strategy names the owned transaction, preview-by-default behavior, exact-target/backup/rollback guards, and the sole opaque `opaque-owned-delete` completion receipt.
3. Re-run the fixture-only owned-delete adapter, shared ChatOps/MCP/Hermes parity, and command-contract suites; then run build, full tests, security QA, diff review, and static Sentinel/Dexter/Hermes invariants.
4. Review the evidence and advance this release gate only if all checks pass. No test may invoke the native delete executable or any live deletion.

## Acceptance evidence
- Account Center, Hermes, and Dexter continue to use exactly the owned transaction through the OpenClaw exact-target adapter, with no alternate delete implementation.
- All public documentation and fixture assertions agree that the native receipt remains opaque and target-free.
- Sentinel `/auth` bytes and Hermes/Dexter weekly-only selection remain unchanged.
- No live account, credential, route, session, provider, or service operation occurs.

## Review results
- Spec: passed — reconciled the release gate after AC-08. The public strategy, `/auth` contract, and operations guidance all name the locally owned `codex-auth-delete.py` exact-account transaction shared with Dexter, preview-by-default behavior, exact connected-target confirmation, backup/atomic rollback/authoritative verification, the one target-free `opaque-owned-delete` receipt, and fail-closed `UNPROVEN`.
- Quality: passed — added a hermetic documentation-contract fixture that reads only repository documentation and rejects stale no-native-transaction claims. The direct Hermes receipt fixture and shared ChatOps/MCP/Dexter fixtures remain green; no Sentinel status renderer, weekly-only policy source, Dexter bridge, Hermes bridge, or native helper changed.
- QA: passed — `npm run qa:security` passed 251 Node fixture/mock tests, 2 direct Hermes-plugin fixtures, typecheck/build, 15 Playwright/axe checks, a 134-file secret scan, and `npm audit --audit-level=high` with 0 vulnerabilities. The owned Python helper passed `py_compile` only; it was not executed. No live deletion or provider/runtime mutation occurred.
