# AC-07 — Shared mutation executor
State: READY_FOR_RELEASE_GATE
Candidate: main
Owner: Account Center production coordinator
No-touch: live accounts, credentials, routing, active sessions, system services, Sentinel status/rendering, Hermes/Dexter weekly-only policy, release-gate items other than AC-07, package manifests/lockfiles, and unrelated worktrees.

## Micro-tasks
1. Inventory every current mutation entry point (CLI, `/auth`, MCP, API, and runtime adapter) and identify any path that can bypass the shared executor lifecycle.
2. Define the bounded fixture-only AC-07 contract: preview, exact review confirmation, idempotency claim/replay, redacted durable audit/receipt, and truthful verified versus `UNPROVEN` terminal state.
3. Add adversarial fixture tests for direct adapter calls, token/scope/target mismatches, duplicate idempotency keys, and attempted-but-unproven replay rendering.
4. Make the minimum core/adapter integration change required to route any remaining in-scope mutation through that lifecycle; preserve the existing owned AC-06 delete transaction and its opaque public receipt.
5. Confirm normal Sentinel `/auth` status output is byte-stable and Hermes/Dexter capacity selection remains weekly-only.
6. Run focused fixture tests, full `npm run qa`, `npm run qa:security`, diff review, and independent spec/quality/QA review before release-gate completion.

## Acceptance evidence
- No live deletion, credential change, route change, session change, or provider request is performed.
- Every supported mutation path either uses the shared protected executor or fails closed before a native apply can run.
- Receipts/audit/replay evidence are redacted and target-free at public boundaries; `UNPROVEN` is never rendered as success.
- The owned native delete remains callable only through the AC-06 exact-target adapter contract.

## Review results
- Spec: passed — `account.delete` now uses the same signed review, idempotency claim/replay, redacted durable operation lifecycle as protected route changes; only the owned OpenClaw adapter can invoke the local exact-account transaction.
- Quality: passed — `npm test` passed 236 tests, including the fixture-only delete lifecycle/replay test; `git diff --check` passed. Sentinel `/auth` rendering and Hermes weekly-only capacity tests remained green.
- QA: passed — `npm run qa` passed (including 15 accessibility/browser tests), and `npm run qa:security` passed (secret scan and `npm audit --audit-level=high`: 0 vulnerabilities).
- No live deletion, account change, routing change, provider request, or service operation was invoked.
