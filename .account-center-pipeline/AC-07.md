# AC-07 — Shared mutation executor release gate

State: MERGED
Candidate: `main` after verified AC-04 and AC-06 preservation review
Owner: Account Center production coordinator

## Non-negotiable boundary

Preserve the locked Hermes/Dexter full Sentinel `/auth` format, Hermes/Dexter weekly-only policy, the owned native exact-account delete transaction at `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`, its single opaque receipt contract, and the working Dexter `/auth delete` command. This gate is fixture/mock-only for destructive paths: no native helper execution, live credential deletion, interactive login, provider request, credential/store write, routing mutation, or runtime service operation.

## Bounded execution plan

1. Inventory every Account Center mutation command and verify that its apply path is mediated by the shared preview/review/capability/idempotency/audit/receipt/post-verification executor rather than a direct adapter or transport call.
2. Exercise fixture/mock adversarial cases for missing or replayed review capability, unsafe scope, duplicate idempotency key, adapter failure, and unproven verification; assert a target-free fail-closed result and no native process invocation.
3. Re-run the owned-delete fixture suite and contract consumers (CLI, ChatOps/Dexter, MCP, and Hermes) to prove the exact helper path, opaque two-result receipt boundary, and `--apply` guard remain intact.
4. Run full QA/security, static compilation of the owned helper and Hermes bridge, diff review, and preservation review. Merge only if every gate is green.

## Acceptance evidence

- All mutation applies use the shared lifecycle; no consumer can bypass review, executor capability, idempotency, audit/receipt, and post-verification.
- Destructive failure and hostile-input paths are fixture/mock-only, target-free, and fail closed without launching the owned helper.
- The owned AC-06 transaction, one opaque receipt contract, Sentinel `/auth`, weekly-only policy, and Dexter `/auth delete` remain unchanged.

## Review results

- Spec: passed — `executeAccountCenterCommand` is the sole protected lifecycle boundary for the supported route and owned-delete actions: it binds canonical target/scope/provider/runtime to review confirmation, mints an unexported capability only after an idempotency claim, and stores only redacted terminal evidence. Unowned account/model mutation vocabulary fails closed before an adapter can run.
- Quality: passed — focused fixture/mock tests covered missing, forged, stale, widened, malformed, cross-runtime, and unobserved scopes; missing confirmed delete review; replay; unproven outcomes; direct adapter capability forgery; native receipt malformed/unverified/mismatched results; and exact opaque receipt transport parity. No test invoked the owned helper.
- QA/security: passed — `npm run qa:security` completed 260 Node fixture/mock tests, 19 Hermes-plugin tests, typecheck/build, 15 Playwright/axe tests, a 148-file secret scan, and `npm audit --audit-level=high` with zero vulnerabilities. Static `py_compile` passed for the owned helper and Hermes bridge; `git diff --check` passed.
- Preservation: passed — the owned helper remains a regular owner-only `0600` file with reviewed SHA-256 `4c09c926e94500f02f34a19ca80fbec280003227588d2b4f0d1d1085ee7fba37`; CLI, ChatOps/Dexter, MCP, and Hermes fixture contracts retained the exact two-result opaque receipt boundary, locked Sentinel `/auth`, and weekly-only policy.

No live deletion, native helper execution, interactive login, provider request, credential/store write, route mutation, session/service operation, or runtime mutation is permitted in this gate.

Result: MERGED after independent spec, quality, QA/security, and preservation review.
