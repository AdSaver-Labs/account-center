# AC-04 — Reauth completion-evidence release gate

State: MERGED
Candidate: main after verified AC-03 release
Owner: Account Center production coordinator

## Non-negotiable boundary

Preserve the locked Hermes/Dexter full Sentinel `/auth` format, Hermes/Dexter weekly-only policy, the owned exact-account delete transaction at `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`, its one opaque receipt contract, and the working Dexter `/auth delete` command. This gate is fixture-only: do not execute native helpers, live deletion, interactive login, provider/runtime mutation, credential or store writes, routing changes, sessions, or services.

## Bounded execution plan

1. Inventory only the existing pure `reauth-proof` and `reauth-completion-evidence` modules, their core exports, and public serialization paths; do not alter runtime adapters or mutation stores.
2. Confirm that readiness can be `VERIFIED` only for an exact fresh proof bound to a pending `reauth` challenge, and that it expressly represents a future native transaction requirement rather than applied authentication.
3. Exercise adversarial fixture cases for absent, malformed, inherited, secret-bearing, stale, future, terminal, and mismatched evidence; all must serialize to one target-free `UNPROVEN` outcome.
4. Re-run preservation fixtures for the owned AC-06 transaction/opaque receipt, Hermes bridge, normal Sentinel `/auth`, weekly-only capacity, and Dexter command contract.
5. Run focused fixtures, full QA/security, static Python compilation only, diff review, and merge only after independent spec, quality, and QA results are green. If an internal issue is found, retain this candidate, write a materially different bounded resolution record, leave this checkpoint `PLANNED`, and execute it.

## Acceptance evidence

- Reauth completion readiness is a pure fixture proof assessment and can never be represented as a native reauthentication, credential write, route change, or account mutation.
- Every public result is fixed and target/credential/proof/diagnostic-free; hostile input fails closed.
- AC-06 remains an owned native exact-account transaction with one opaque receipt contract, and no test can launch it.
- Sentinel `/auth` output, Hermes/Dexter weekly-only policy, and working Dexter `/auth delete` command remain unchanged.

## Review results

- Spec: passed — `assessReauthCompletionReadiness` is a pure core export that maps only independently validated, fresh proof for a pending reauth challenge to `VERIFIED` with `native_transaction_required`; it does not import a runtime adapter or mutation store and does not claim an applied reauthentication.
- Quality: passed — focused compiled fixtures verified exact proof binding and a fixed target-free `UNPROVEN` output for absent, malformed, inherited, secret-bearing, stale, future, terminal, mismatched, and throwing inputs. Focused adapter/CLI/Hermes fixtures also retained the AC-06 exact owned transaction and the two-result opaque receipt boundary.
- QA/security: passed — `npm run qa:security` completed 260 Node fixture/mock tests, 19 Hermes-plugin tests, typecheck/build, 15 Playwright/axe tests, a 146-file secret scan, and `npm audit --audit-level=high` with zero vulnerabilities. `python3 -m py_compile` passed for the owned helper and Hermes plugin; `git diff --check` passed.
- Preservation: normal Sentinel `/auth`, Hermes/Dexter weekly-only capacity, and the working Dexter `/auth delete` command remained covered. The native helper was never launched.

No live deletion, interactive login, provider request, credential/store write, route mutation, session/service operation, or native helper execution occurred.
