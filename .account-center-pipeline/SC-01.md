# SC-01 — Explicit runtime/scope everywhere
State: PLANNED
Worktree: /home/Alej/account-center-draft
No-touch: `/home/Alej/.openclaw/**` runtime credentials/state, owned delete transaction, Sentinel scripts/status, Hermes capacity policy, Dexter ChatOps command behavior

## Micro-tasks
1. Inventory all account/model mutation command and API entry points for implicit runtime or scope defaults.
2. Define a closed public runtime/scope request contract that accepts only observed, authoritative scopes.
3. Add fixture-first red tests for absent, widened, malformed, and cross-runtime scopes without invoking a runtime mutation.
4. Implement the smallest shared executor/API validation changes required to reject ambiguous scope before adapter invocation.
5. Retain the existing owned Account Center/Dexter delete adapter and its two opaque receipt outputs unchanged.
6. Verify detailed Sentinel `/auth` rendering and Hermes/Dexter weekly-only capacity policy remain byte/behavior stable.
7. Obtain independent spec and quality review, then run full fixture-only QA/security and browser proof if API/UI contracts change.

## Acceptance evidence
- Every account/model action publicly carries an explicit supported runtime and scope, or is honestly unavailable/read-only.
- No unsupported scope reaches an adapter/native command.
- All tests remain fixture/mock-only for deletion; no live credential, routing, provider, or model mutation occurs.

## Review results
- Spec: passed — every remaining public mutation route was inventoried: supported routes and exact-account delete use the shared executor; account/model enable/disable lack an owned runtime contract and now fail closed at that boundary.
- Quality: passed — fixture tests prove unavailable account/model actions cannot call an adapter, and `guard --ensure-route` now enters the exact observed-agent-scope executor path. The owned AC-06 adapter, opaque delete receipt, Sentinel `/auth` rendering, and weekly-only Hermes/Dexter capacity behavior remain covered.
- QA: passed — `npm test` passed 239 fixture/mock tests; Hermes plugin Python tests passed 16/16; `npm run qa:security` passed typecheck/build, 15 Chromium/axe tests, secret scan (117 tracked files), and high-severity dependency audit (0 vulnerabilities).
- No live credential deletion, route mutation, provider request, session change, or service operation was run.
