{
  "schemaVersion": "account-center.pipeline-state.v1",
  "activeCheckpoint": "AC-08",
  "state": "MERGED",
  "updatedAt": "2026-07-29T17:33:46Z",
  "candidate": "main candidate after AC-07 merge; AC-08 closes the direct Hermes plugin fixture gap while preserving the AC-06 owned exact-account delete transaction and all locked Sentinel/weekly-only behavior.",
  "blockerResolutionPlan": {
    "kind": "ac08-hermes-owned-delete-contract-fixture-r1",
    "boundedSteps": [
      "Add a hermetic Python fixture that imports the real Hermes plugin against a temporary Account Center root containing only the versioned receipt contract and a presence-only ChatOps stub.",
      "Stub the plugin subprocess boundary and assert that Hermes forwards only the two canonical target-free contract texts, rejecting injected, malformed, failed, or unavailable transport output as canonical UNPROVEN.",
      "Run the fixture as part of the repository test/QA path alongside the existing Account Center/Dexter/MCP fixture suite; use no live helper execution, provider request, credential/store access, or interactive login.",
      "Review the diff and rerun QA/security before recording the release-gate result; preserve byte-stable Sentinel /auth output, weekly-only policy, and the working Dexter /auth delete command."
    ],
    "liveDeletionTestsForbidden": true,
    "interactiveLoginForbidden": true,
    "preserveWeeklyOnlyPolicy": true,
    "preserveSentinelAuthFormat": true,
    "preserveOwnedDeleteTransaction": true,
    "preserveOpaqueNativeDeleteReceiptContract": true,
    "preserveDexterAuthDeleteCommand": true
  },
  "review": {
    "spec": "passed",
    "quality": "passed",
    "qa": "passed",
    "evidence": [
      "Spec review: the real Hermes plugin loads the one versioned Account Center owned-delete receipt contract and accepts only its exact applied or UNPROVEN public delete text.",
      "Quality review: the hermetic fixture replaces subprocess.run before every /auth delete request, asserts the ChatOps bridge boundary, and fails closed for injected, nonzero, and unavailable outputs; the owned helper remains unexecuted.",
      "QA review: npm run qa:security passed 249 Node fixture/mock tests, 2 direct Hermes-plugin fixtures, typecheck/build, 15 Playwright/axe checks, a 132-file secret scan, and npm audit --audit-level=high with 0 vulnerabilities.",
      "Static py_compile passed for the Hermes plugin and owned Python helper. No live deletion, provider request, credential/store operation, interactive login, Sentinel status operation, or route/session mutation occurred."
    ]
  },
  "previousMerge": {
    "checkpoint": "AC-06",
    "commit": "c7b5c55",
    "branch": "main",
    "verified": true,
    "scope": "Owned exact-account delete integration verified fixture-only; no live credential deletion executed."
  },
  "merge": {
    "commit": "4cf0cce",
    "branch": "main",
    "scope": "AC-08 direct Hermes plugin fixture closes the receipt-contract coverage gap; the owned exact-account delete transaction and opaque receipt boundary are preserved.",
    "recordedBy": "pushed to origin/main and verified against remote ref"
  }
}
