# AC-08 — Direct Hermes owned-delete receipt fixture
State: PLANNED
Candidate: main
Owner: Account Center production coordinator

## Bounded resolution

The versioned Account Center receipt contract and ChatOps/MCP fixtures already establish the owned exact-account delete transaction and its target-free public results. This checkpoint closes the remaining direct-Hermes-plugin fixture gap without changing the transaction, runtime state, or policy.

1. Import the real Hermes plugin against a temporary fixture Account Center root that contains only the versioned owned-delete receipt contract and a presence-only ChatOps file.
2. Stub its subprocess boundary; do not invoke Node, ChatOps, the native Python helper, OpenClaw, providers, or any live account/store.
3. Prove the plugin forwards the contract's exact applied and `UNPROVEN` text only, and converts injected, nonzero, and unavailable transport results to the exact fail-closed `UNPROVEN` text.
4. Make this hermetic fixture part of the normal repository test/QA path. Re-run full QA/security and review the diff before recording completion.

## Invariants

- The native apply transaction remains `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`; no upstream OpenClaw CLI-delete command is needed.
- No test executes the native helper or performs a live deletion.
- Preserve the locked full Sentinel `/auth` format, Hermes/Dexter weekly-only policy, working Dexter `/auth delete` command, exact connected-target checks, and the sole opaque receipt contract.

## Review results

- Spec: passed — the real Hermes plugin loads the same versioned Account Center receipt contract and permits only its canonical applied or `UNPROVEN` public delete texts; native helper output never crosses the bridge.
- Quality: passed — a hermetic fixture replaces the plugin subprocess before each delete request, checks the ChatOps invocation boundary, and fails closed for injected, nonzero, and unavailable transport results. The owned helper remains unexecuted.
- QA: passed — `npm run qa:security` passed 249 Node fixture/mock tests, the new 2-test Hermes-plugin fixture, TypeScript typecheck/build, 15 Playwright/axe checks, the 132-file secret scan, and high-severity `npm audit` with 0 vulnerabilities. Static `py_compile` passed for the Hermes plugin and owned helper.
