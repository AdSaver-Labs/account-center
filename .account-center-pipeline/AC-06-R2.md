# AC-06-R2 — Opaque delete transport and owned-helper integrity remediation
State: PLANNED
Candidate: main at `d893044`
Owner: Account Center production coordinator
No-touch: live accounts, credentials, routes, sessions, provider endpoints, production runtime stores, native helper behavior, Sentinel `/auth` renderer/format, working Dexter `/auth delete` semantics, and Hermes/Dexter weekly-only eligibility policy.

## Bounded micro-tasks
1. Preserve the existing AC-06 candidate and receipt schema; do not execute the owned Python helper or any live deletion.
2. Make malformed Dexter `/auth delete` transport input emit only the existing canonical opaque `UNPROVEN` delete result, matching Hermes’ fail-closed boundary.
3. Add fixture-only transport coverage for malformed delete input, asserting no target/path material crosses the public output boundary.
4. Add a bounded owned-helper integrity gate: require the fixed local helper path to be an owner-owned, non-symlink regular file with no group/other access and the recorded SHA-256 content digest before an authorized delete runner may be invoked.
5. Add injected fixture tests for absent, symlink-like, unsafe-mode, wrong-owner, and hash-mismatched helper evidence, proving each blocks before runner invocation; retain the existing verified fixture receipt test.
6. Include the entire Hermes plugin fixture suite in the deterministic `npm test` gate, including descriptor-relative blocked-receipt filesystem safety tests.
7. Run focused tests, full deterministic QA/security/browser proof, static compilation, staged diff review, and independent spec/quality/QA review; confirm Sentinel `/auth` and weekly-only policy sources remain unchanged.

## Acceptance evidence
- Dexter ChatOps, MCP, and Hermes expose exactly the versioned applied or `UNPROVEN` delete contracts, including malformed delete input; no native diagnostics, identity, target digest, or path reaches public output.
- Account Center runs the owned local helper only after exact-target/capability checks and helper integrity evidence; every integrity failure blocks without runner invocation.
- Fixture suites never run the native helper or modify a live credential store.
- The default test command covers all Hermes plugin safety fixtures; locked Sentinel `/auth`, the working Dexter delete command, and weekly-only policy remain unchanged.

## Review results
- Spec: passed — malformed Dexter delete input now enters the same canonical target-free `UNPROVEN` receipt boundary Hermes uses; successful application remains restricted to the unchanged owned local transaction and `opaque-owned-delete` receipt.
- Quality: passed — the fixed owned helper path is fail-closed unless it is a current-user-owned, non-symlink regular file with no group/other access and the reviewed SHA-256 digest. Fixture seams prove absent/symlink/shared-mode/wrong-owner/hash-mismatch evidence blocks before any runner call. The full Hermes fixture suite is now part of `npm test`.
- QA: passed — `npm test` passed 253 Node tests and all 18 Hermes Python fixtures; `npm run qa:security` passed typecheck/build, 15 Playwright/axe checks, a 137-file secret scan, and high-severity audit with 0 vulnerabilities. Static `py_compile` passed for Hermes, receipt persistence, and the owned helper. No native helper, live delete, provider, account, route, session, or store operation was executed.
