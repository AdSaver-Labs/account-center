# CP-04 R1 — AC-06 owned delete bridge parity
State: PLANNED
Candidate: current `main` after independent fixture-only verification of Account Center, Hermes, and Dexter delete receipt parity
Owner: Account Center production coordinator

## Bounded blocker-resolution plan
1. Inspect—but never execute—the owned local `codex-auth-delete.py` helper: it must be a regular owner-only file matching the pinned reviewed SHA-256.
2. Use fake runners plus fixture receipt payloads to prove Account Center invokes only that exact transaction following shared executor confirmation and exact connected-target resolution; project no native target digest, stderr, path, or credential data.
3. Use hermetic fixture roots to prove Hermes and Dexter consume the same `owned-delete-receipt.v1` applied/unproven text, and reject every non-exact success transport.
4. Run focused fixture tests and deterministic full QA/security; inspect preserved Sentinel `/auth` rendering and weekly-only policy. No helper invocation, live delete, provider request, login, routing/model mutation, or runtime operation is permitted.

## Acceptance evidence
- Account Center references the immutable owned helper path and trust pin; fake-runner tests show the sole accepted public native completion object is `{action: account.delete, state: DELETED, receipt: opaque-owned-delete}`.
- Hermes and Dexter fixture tests consume `contracts/owned-delete-receipt.v1.json` and reject altered output.
- No destructive helper execution occurs.
- Full deterministic QA/security passes and locked Sentinel `/auth` plus weekly-only exports remain covered.

## Review results
- Spec: passed — Account Center’s OpenClaw adapter has one immutable owned helper path and accepts only a verified native result as the target-free `opaque-owned-delete` receipt. Exact target resolution, capability gating, trusted-helper checks, malformed/unverified evidence, and private-field suppression are fixture covered.
- Quality: approved — hermetic Account Center/Hermes/Dexter ChatOps and MCP tests consume the same `owned-delete-receipt.v1` fixture contract and reject modified success output. Diff review found no implementation changes to the locked Sentinel `/auth`, weekly-only export, adapter, bridge, or contract surfaces.
- QA/security: passed — helper compiled only; 78 focused Node tests and 11 direct Hermes tests passed; full `npm run qa:security` passed with 268 Node tests, 19 Hermes tests, 15 accessibility tests, a 159-file secret scan, and zero high audit vulnerabilities. No live deletion or runtime/provider mutation occurred.
