# CP-04 R2 — AC-06 release-gate evidence reconciliation

State: PLANNED
Owner: Account Center production coordinator

## Internal issue

The currently locked coordinator state names `CP-04` as active while also marking
it `MERGED`. That is an internal release-record integrity defect: it obscures
whether the protected AC-06 owned-delete requirement is still the one active
checkpoint or a completed gate. It is not an upstream OpenClaw limitation and
must not reclassify, skip, replace, or execute the owned transaction.

## Bounded resolution plan

1. Preserve the existing merged candidate and record this distinct coordinator
   reconciliation plan under the exclusive pipeline lock before running checks.
2. Re-read the Account Center adapter, CLI contract loader, Dexter ChatOps/MCP,
   and direct Hermes-plugin entry points to prove they all use the one versioned
   opaque receipt contract and that the adapter's only native delete target is
   the installed owned `codex-auth-delete.py` helper.
3. Verify the helper is a regular owner-only file and compile-check it only;
   never invoke it. Run focused fixture tests for Account Center/Dexter/Hermes,
   then the full deterministic security QA path. All delete subprocesses must
   remain fake runners or temporary fixture transports.
4. Review the resulting diff and protected invariants: full Sentinel `/auth`
   format, the working Dexter `/auth delete` command, exact connected-target
   checks, and Hermes/Dexter weekly-only policy. If green, commit/push only the
   release evidence and change the active state to `MERGED`; otherwise retain
   this candidate as `PLANNED` with the collected failure evidence.

## Invariants

- AC-06 is owned by the local exact-account transaction at
  `/home/Alej/.openclaw/workspace/3-Resources/codex-account-ops/scripts/codex-auth-delete.py`.
  No upstream OpenClaw CLI delete command is required.
- One target-free public receipt contract is authoritative:
  `account-center.owned-delete-receipt.v1` / `opaque-owned-delete`.
- No live deletion, login, provider request, credential/store write, route or
  model mutation, browser action, or runtime-service operation is permitted.
